# try to find rbenv
for dir in "$HOME/.rbenv" "/usr/local/rbenv"
do
  if [ -x "$dir/bin/rbenv" ]
  then
    # add it to the PATH
    export PATH="$dir/bin:$PATH"
    # enable shims and autocompletion
    eval "$(rbenv init - 2>/dev/null)"
    break
  fi
done

